<section class="value-props">
  <div class="section-header">
    <div>
      <p class="section-eyebrow">{{ section.settings.eyebrow }}</p>
      <h2>{{ section.settings.title }}</h2>
    </div>
  </div>
  <div class="value-props__grid">
    {% for block in section.blocks %}
      <div class="value-prop" {{ block.shopify_attributes }}>
        <span class="value-prop__icon">{{ block.settings.icon }}</span>
        <h3>{{ block.settings.title }}</h3>
        <p>{{ block.settings.text }}</p>
      </div>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "Value props",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Notre méthode"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Des conseils utiles, pas du marketing"
    }
  ],
  "blocks": [
    {
      "type": "prop",
      "name": "Prop",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "default": "🧭"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Conseil terrain"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "On recommande seulement ce qui colle à ton usage."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Value props",
      "blocks": [
        { "type": "prop" },
        { "type": "prop" },
        { "type": "prop" }
      ]
    }
  ]
}
{% endschema %}
